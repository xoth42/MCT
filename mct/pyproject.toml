[project]
name = "mct"
version = "0.1.0"
description = "Monte carlo trajectory deriver/tester"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["scipy", "sympy", "numpy", "matplotlib", "numba"]